# v1.3.1 — GitHub Actions CI + 開發流程文件

**日期**：2026-04-05

## 主題

建立 GitHub Actions 持續整合（CI），並在 CLAUDE.md 整理完整的每次迭代開發流程。

---

## 新增

- `.github/workflows/pytest.yml`：push / PR 自動觸發，Ubuntu + Python 3.14 + Playwright Chromium，測試完上傳 `report.html` artifact（保留 14 天）

---

## 修改

- `tests/conftest.py`：`headless` 改由 `CI` 環境變數控制（本機 headed，CI headless）
- `pyproject.toml`：新增 `addopts = "--html=report.html --self-contained-html"`
- `CLAUDE.md`：補充完整每次 iteration 開發流程（開 branch → 實作 → 測試 → changelog → commit/push → CI → merge → tag）
