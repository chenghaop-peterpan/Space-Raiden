# v1.6.1 — 文件補齊（README + spec.md）

**日期：** 2026-04-19
**類型：** patch

---

## 主題

補齊自 v1.1.0 起累積的文件欠債：README.md 與 docs/spec.md 均更新至 v1.6.0 功能；CLAUDE.md 新增文件維護規則。

---

## 修改

### README.md
- 新增「遊戲模式」section（玩家模式 / AI 模式 / 遊戲設定說明）
- 補全操控按鍵表（Shift Dash、H HUD、B Benchmark、E CSV、R 安全距離、Esc 返回）
- 補充 AI 模式說明（策略比較表、Epoch 批次、API 範例）
- 遊戲機制新增 Dash 說明
- 測試命令更新為 `CI=true .venv/Scripts/pytest.exe tests/ -v`
- 檔案結構更新（含 changelog 列表）

### docs/spec.md
- 狀態機新增 `game_settings` 狀態，選單改為三項
- 新增「Dash 機制」章節（dashConfig、dashGhosts、fireDash() 行為）
- 新增「可插拔 AI 策略架構」章節（AI_STRATEGIES 介面、各策略 Dash 門檻）
- 新增「Epoch 批次執行系統」章節（epochConfig、advanceEpoch() 流程）
- 更新 `getGameState()` 新增 `dashCooldown` 欄位
- 更新 `executeCommand()` 新增 `cmd.dash`
- 更新渲染管線（第 9 步：Dash 殘影）
- 更新 Debug HUD 顯示項目（safetyViz、dash CD）
- 新增「安全距離視覺化」說明（R 鍵）
- 版本管理規範新增文件維護步驟

### CLAUDE.md
- 每次 Iteration 開發流程新增步驟 4b：更新 README.md 與 docs/spec.md

---

## 測試

- 83 tests passed（純文件更新，無 regression）
