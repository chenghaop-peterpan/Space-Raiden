# v1.3.5 — CI paths filter + 備忘錄同步

**日期**：2026-04-06

## 主題

加入 CI paths filter，避免 changelog / docs 變動觸發不必要的測試；同步更新備忘錄。

---

## 修改

- `.github/workflows/pytest.yml`：新增 `paths` filter
  - 觸發條件：`space_dodge.html` / `tests/**` / `requirements.txt` / `.github/workflows/**`
  - changelog、docs、roadmap 等純文件變動不再觸發 CI
- `docs/roadmap.md`：更新 CI backlog（paths filter 標為完成）、加入 v1.3.5

---

## 備注

smoke-first 分層執行保留在 backlog，待測試套件規模增長後再評估。
