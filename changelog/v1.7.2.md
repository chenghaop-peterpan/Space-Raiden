# v1.7.2 — 測試重構：移除低 priority UI 測試，補 dashCooldown unit test

**日期：** 2026-04-19
**類型：** patch

---

## 主題

移除驗證 DOM 顯示文字的脆弱 UI 測試，改以邏輯層 unit test 取代。

---

## 修改

### `tests/test_functional.py`
- 移除 `test_dash_hud_shows_cooldown`（驗 DOM textContent，易因文字改動而壞）
- 移除 `test_dash_hud_shows_ready`（同上）

### `tests/test_unit.py`
- 新增 `test_dash_cooldown_decrements_each_frame`：每次 `update()` dashCooldown 遞減 1
- 新增 `test_dash_cooldown_clamps_at_zero`：dashCooldown 不會低於 0

---

## 測試

- 89 tests passed（數量不變，品質提升）
